# 视频播放器「双击打开慢 / 空窗不播放」问题分析

> 来源：File Searcher 工作区排查（2026-08-15）
> 结论：两个症状、两个独立原因。File Searcher 无辜，问题都在播放器侧。

---

## 症状描述

从 File Searcher 双击视频文件：
1. **要很久才有反应**（窗口迟迟不出现）
2. **窗口起来了，但不播放**（只有空窗口）

在 Windows 资源管理器双击同一个文件：快速打开、正常播放。

---

## 症状 1：慢 —— 播放器冷启动耗时

### 结论
File Searcher 发打开请求是毫秒级，慢的是**播放器自己的冷启动**。

### 证据链

**File Searcher 侧（无辜）：**
- `_shell_open` 用 `ShellExecuteExW` + `SEE_MASK_FLAG_DDEWAIT | SEE_MASK_NOASYNC`，**后台线程**发起，发完立即返回，UI 完全不阻塞
- `debug.log` 里一条 `[open-error]` 都没有——FS 发打开请求从未失败

**播放器侧（真凶）：**
- `shutdown.log`（位于 `%APPDATA%\com.hjf.videoplayer\shutdown.log`）铁证：
  每次 `app starting` → `setup: main window found, showing` **固定 0.6 ~ 10 秒**（2026-08-15 15:30 那次达 10 秒）
- 这 0.6~10 秒就是用户感受到的「很久没反应」

**为什么资源管理器双击快？**
- 那时播放器**已在后台运行**，走的是单实例转发（已有实例 → put path → emit），毫秒级弹窗
- 从 File Searcher 双击时若播放器没在跑，就是完整冷启动（WebView2 初始化 + Rust setup + probe 分辨率 + DB 初始化），自然慢

### 启示
冷启动慢本身不是 bug（Tauri/WebView2 应用冷启动就是这个量级）。**真正的 bug 是症状 2**——冷启动后还不播放。

---

## 症状 2：空窗不播放 —— 单实例转发的原子性缺陷（核心 bug）

### 结论
这是播放器**单实例转发链路的事件丢失问题**，跟 File Searcher 怎么调用完全无关。

### 缺陷位置：`src-tauri/src/lib.rs` 单实例回调（约 146~150 行）

```rust
if let Some(path) = media::video_file_from_args(argv.iter().skip(1)) {
    app.state::<pending_open::PendingOpen>().put(path);  // ← ① put 进 mailbox 了
    let _ = app.emit("open-file-available", ());         // ← ② emit 失败被 let _ 吞掉
}
```

**缝隙：**
- `emit` 返回 `Result`，`let _ =` 把错误吞了
- 如果主实例此时 **WebView2 还没起来、前端还没执行到 `listen("open-file-available")`**（`src/App.vue` 第 393 行），这个事件就**永久丢失**
- 文件路径躺在 mailbox（`pending_open`）里，但**没人被叫醒去取** → 窗口空着、不播放

### 为什么 onMounted 的兜底没救场？

`src/App.vue` 第 397 行有个兜底：
```ts
const pending = await invoke<string | null>("take_pending_open_file").catch(() => null);
if (pending) await openFileWhenReady(pending);
```
**但它只在组件挂载时取一次。** 单实例场景下主实例早就挂载完了，这个兜底根本不会跑——mailbox 里的文件永远没人取。

### 为什么资源管理器双击没这问题？

- 双击时播放器通常已在运行、前端早已挂载好，`listen` 是活的，事件一发就收到
- 从 File Searcher 双击时如果**恰好赶上主实例刚被 kill、WebView2 正在初始化**，就踩中这个缝隙

---

## 修复方案（推荐：前端 focus 兜底，不依赖后端事件）

### 思路
不再信任后端的 `open-file-available` 事件（它会丢），改为**前端每次窗口获得焦点时主动 `take_pending_open_file` 一次**。这样无论 emit 丢没丢，只要用户切到窗口，pending 的文件就会被播出来。

### 改动点：`src/App.vue` 的 `onMounted`

在现有 `listen("open-file-available", ...)` 之后，**加一路窗口 focus 监听**：

```ts
// 现有：后端事件触发（会丢，保留作为快速路径）
unlistenOpenFileArgv = await listen("open-file-available", () => {
  void consumePendingOpen();
});

// 新增：窗口 focus 时主动消费 mailbox 兜底（事件丢失时的最后一道防线）
const unlistenFocus = await appWindow.onFocusChanged(({ payload: focused }) => {
  if (focused) void consumePendingOpen();
});
// onUnmounted 里记得 unlistenFocus?.();
```

这样形成**三路互补**：
1. `onMounted` 里的 `take_pending_open_file`（冷启动）
2. `listen("open-file-available")`（热路径，事件没丢时毫秒级）
3. **窗口 focus 兜底**（事件丢了，用户切窗口时救回来）—— 新增

### 可选加固（Rust 侧）
如果想更稳，可把 `emit` 失败的情况也兜住。但前端 focus 兜底已足够覆盖所有「事件丢失」场景，Rust 侧可不动。

---

## 涉及文件清单

| 文件 | 角色 |
|---|---|
| `src-tauri/src/lib.rs` | 单实例回调（put + emit，emit 会丢）|
| `src-tauri/src/pending_open.rs` | mailbox（`put`/`take`，`take_pending_open_file` 命令）|
| `src-tauri/src/media.rs` | `video_file_from_args` 从 argv 提取视频路径 |
| `src/App.vue` | 前端消费逻辑（`consumePendingOpen` / `openFileWhenReady` / `onMounted`）|
| `src/composables/useMpv.ts` | `openFile` 播放流程、`isReady` 初始化门 |

---

## 验证方法

1. 彻底杀掉所有 `video-player.exe` 进程
2. 从 File Searcher 双击一个视频（确保触发冷启动）
3. 预期：窗口起来后**自动开始播放**（不再空窗）
4. 再双击另一个视频（单实例转发路径）：预期快速切换播放

---

## 备注：File Searcher 侧无需任何改动

- `_shell_open`（`file_searcher.py` 第 425 行）用 `ShellExecuteExW` + DDE 等待，是 Windows 上最标准的「等同资源管理器双击」的打开方式
- 后台线程发起，不阻塞 UI，失败会写 `debug.log` 的 `[open-error]`——目前零失败记录
